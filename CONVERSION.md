# 文档转换说明

PaperDesk 以 A4 图文作业排版为主。请保存工程文件，以便继续修改图片位置、裁剪范围和文本框；交作业时优先导出 PDF，保留画布上的版式。

## 支持的方向

| 操作 | 当前行为 |
| --- | --- |
| 工程 → PDF | 保留 A4 页面尺寸、文本框与图片位置；文字作为文字导出。 |
| DOCX → 工程 | 导入正文、基础文字样式和嵌入图片，按阅读顺序重新排入 A4 页面。 |
| Markdown / TXT → 工程 | 导入 UTF-8 文本，长内容自动分页。 |
| 工程 → DOCX | 导出可编辑文字和真正嵌入的 PNG 图片，按每页从上到下、从左到右排列。 |
| 工程 → Markdown | 导出基础 Markdown 和相邻图片资源目录。 |
| DOCX ↔ Markdown | 先导入工程，再导出所需格式。 |
| DOCX / Markdown → PDF | 先导入并检查 A4 排版，再导出 PDF。 |

本版本不提供 PDF 转 Word、PDF 转 Markdown 或 OCR。

## DOCX 的范围

支持段落、中文和英文、字号、可用字体、加粗、斜体、下划线、文字颜色、基础对齐和嵌入图片。图片导出会应用工程内的裁剪。列表按可见编号或圆点导入。表格中的段落按单元格顺序展开。

导出 DOCX 使用标准 OOXML 文字段落与图片媒体资源，不会把整页变成一张图片。工程中的手动页分隔会写入 DOCX；Word 仍可能因字体、段落和图片流式排版而增加页面。

不保证还原外部 Word 文件的浮动对象坐标、文字环绕、复杂表格、公式、图表、艺术字、批注修订、目录、页眉页脚和复杂多级列表。表格结构、特殊分页设置等不会保留。使用本机未安装的字体时会回退到系统字体。外链图片需要先在 Word 中嵌入；无效或缺失图片会明确报错。

## Markdown 的范围

导入支持 ATX 标题（`#`）、段落、基础有序/无序列表、行内加粗和斜体、行内代码、围栏代码，以及 `![说明](相对路径)` 图片。标题转换为对应字号；导出时根据字号和加粗推断一级到三级标题。Markdown 不承载纸张尺寸、图片自由坐标、裁剪参数、字体、颜色或精确分页。

列表导入为可编辑的文字项目，不提供复杂嵌套列表结构。表格、HTML、引用、公式、引用式图片等扩展语法不在当前基础转换范围内。代码块导入为等宽文字，导出可能作为普通文本。导出的分页标记 `<!-- PaperDesk page break -->` 会在再次导入时恢复页分隔。

图片只读取 Markdown 所在目录及其子目录内的相对路径。不会下载网络图片，也不允许通过 `../` 或符号链接读取目录外的文件。缺失图片会报错。含空格的手写图片路径可用尖括号包围，例如 `![图](<assets/my image.png>)`。

每次导出 Markdown 都会创建独立的 `文件名-assets-随机标识` 目录，避免覆盖之前导出的图片。发送 Markdown 时需要同时发送对应资源目录；若手动删除旧 Markdown，可一并清理它所引用的旧资源目录。

## 检查建议

重新导入 DOCX 或 Markdown 后，检查标题、分页、图片与列表。复杂 Word 作业需要保留原文件。PDF 是本程序提交固定版式作业的主要输出格式，工程文件是再次编辑的主要存档格式。

实现依据：[Apple 的富文本接口](https://developer.apple.com/documentation/foundation/nsattributedstring/documenttype)提供基础文字格式能力；本机验证发现其原生 DOCX 导出会丢失图片附件，因此 PaperDesk 使用独立的 OOXML 打包与解析来保存真实图片。
