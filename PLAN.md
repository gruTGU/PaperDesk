# PaperDesk 开发计划

## 已确认的范围（2026-09-10）

- 仅用于个人 Mac M2，本地处理，中文界面。
- 主要场景：新建图文作业、自由排图片、导出 PDF。
- 页面始终为 A4（210 × 297 mm）；缩放与屏幕上的页间距不改变导出尺寸。页间距指两张纸在操作台上的空隙。
- 图片导入、拖动、缩放、矩形裁剪、拼图；PNG/JPEG/HEIF（HEIC）转存；按期望文件大小压缩。
- 基础文字编辑，DOCX 与 Markdown 基础导入、导出，可再导出 PDF。取消 PDF 转回 DOCX / Markdown 和 OCR。
- 接受命令启动，之后提供 Mac .app。

## 0.2.0 新增范围（2026-09-11）

- A4 纸张内部四边距按 mm 独立设置，旧版页间距仍控制纸张之间的空隙。新文字、拼图及基础 DOCX 使用内容区域，已有对象不自动重排；PDF 保持现有坐标与 A4 尺寸。
- 任意缩放比例可以左右平移：横向滚动条、触控板、Shift + 滚轮、“平移”模式拖动，以及画布获得焦点后按住空格拖动。输入文字时空格保持正常输入；缩放围绕当前视图中心。
- 类似浏览器的多文档标签与独立窗口，每份文档独立保存、撤销和恢复；支持多文件一次打开、快捷键切换与关闭，以及从标签拆出独立窗口。
- 顶部“另存为”入口与 ⇧⌘S，另选文件名保存副本；存储空间面板列出内存预览、应用磁盘缓存及恢复备份，分别清理并保护打开文档正在使用的备份。

## 技术与边界

Swift + AppKit，系统 ImageIO 与 PDFKit；优先不引入额外本地转换组件。工程文件 `.paperdesk` 保存页面、富文本、原始图片和裁剪信息，作为继续编辑的原件。PDF 固定版式；DOCX 与 Markdown 是内容交换格式，按阅读顺序输出，Markdown 不保存字体、分页和自由摆放坐标。首版是图文排版工具，文字框超出时明确提示；不承诺完整 Word 自动排版、复杂表格、公式、修订或页眉页脚往返兼容。

目标压缩大小解释为“不超过指定上限”。JPEG/HEIC 优先降低编码质量，必要时降低像素尺寸；PNG 不支持同类质量参数，必要时降低像素尺寸。界面显示实际结果，无法满足时明确报错。

## 分阶段交付与验收

1. **基础闭环**：可启动程序、固定 A4 多页、缩放、可调页间距、文字框编辑、工程存取、撤销/重做、自动恢复、PDF 导出。验收：保存重开内容一致，多页 PDF 每页为 A4，缩放与页间距不进入输出。
2. **图片工作流**：多图导入、跨页移动、等比缩放、图层顺序、裁剪、网格拼图、单图与整页图片导出、指定 KB 上限压缩。验收：PNG/JPEG/HEIC 能读写，方向正确，裁剪不毁原件，压缩结果按实际字节数检查。
3. **内容交换**：DOCX/Markdown 基础导入和导出；中文、段落样式和图片的样例检查；导入内容放入 A4 页面。验收：导出的 DOCX 可作为文本文档读取、图片资源存在；Markdown 与本地图片一起保存；不支持的排版明确说明。
4. **交付与检查**：命令启动脚本、arm64 .app、使用说明、自动化自检、图形界面实际操作检查。保存真实验证结果与待办，便于额度不足时续做。
5. **0.2.0 使用体验**：四边页边距、可平移的缩放视图、多文档标签/独立窗口、另存为、缓存与恢复管理。实现、核心 73 项、存储 18 项及内部界面检查已完成；主要新增桌面操作已实际验证，应用重新打包。HEIC 沿用明确跳过状态，具体结果与后续验证边界见 `STATUS.md` 和 `FEATURES-VALIDATION.txt`。

每个阶段更新 `STATUS.md`。后续对话先读本文件、`STATUS.md` 与 `README.md`，即可接续实现。
