# 当前交付与续做记录

更新：2026-09-11，版本 0.2.0。Mac arm64 应用和完整源码已加入多文档与画布功能。本次自动化自检和新增功能的主要桌面操作检查通过；HEIC 等原有待验项目仍保留。

2026-09-12 仓库归档：当前源码、测试和文档作为初始快照保存于 `gruTGU/PaperDesk` 的 `main` 分支，版本标签 `v0.2.0`。对应发布包包含 Mac arm64 App、源码和文档；编译缓存、个人工程及恢复备份不上传。此次只调整文档中的获取方式和 Git 说明，功能与原有测试结论不变。

## 0.2.0 画布、多文档与存储

开发暂告一段落。README 已补充完整开发/学习手册，包括环境与构建、数据结构、各功能实现、操作系统视角、源码阅读路线、修改入口、测试和后续建议。本次只更新文档与交付压缩包，功能版本仍为 0.2.0；后续建议不是已实现功能。

- A4 内部上、下、左、右页边距分别按 mm 设置，旧版“页间距”仍单独控制两张纸之间的操作台空隙。参考线、新文字、拼图及基础 DOCX 分页使用新的内容区域；已有对象保留原位置，A4 尺寸及 PDF 固定版面不变。旧工程缺少边距字段时采用原来的 15 mm。
- 所有缩放比例支持横向滚动条、触控板横向滚动、Shift + 滚轮，以及“平移”勾选后拖动。画布获得焦点且未编辑文字时可按住空格拖动；缩放保留视图中心对应位置。
- 新文档采用标签，可同时打开多个选中文件；⌘T / ⌘N 新标签、⇧⌘N 独立窗口、⌘W 关闭当前标签、Control + Tab / Control + Shift + Tab 切换。“窗口”菜单可把当前标签移到独立窗口。各文档拥有独立撤销、保存及恢复状态。
- 顶部增加“另存为”（⇧⌘S），默认建议副本文件名；保存到不同文件名后，当前标签使用新文件，原文件保留。其他打开标签使用中的文件路径不能覆盖。
- 底部“存储空间”打开独立面板，列出图片预览内存估算、磁盘缓存和恢复备份大小；可分别清理预览、磁盘缓存及选中的未使用恢复备份。使用中的恢复备份受保护，普通工程文件不在清理范围内。
- 每份文档使用 `~/Library/Application Support/PaperDesk/Recovery/*.paperdesk` 下的独立恢复文件，仍读取上一版同级 `Recovery*.paperdesk`。启动可恢复全部备份为标签，暂不恢复时保留备份。
- 核心自检 **73 项断言、0 个失败组**；内部界面测试覆盖多文档生命周期、页边距、平移和对比度；存储自检 **18 项通过**。汇总记录见 `FEATURES-VALIDATION.txt`。HEIC 仍明确跳过，不计为通过。

## 0.1.1 界面修复

- 修复系统使用深色外观时，工具栏和属性面板背景过暗、黑色文字难以辨认的问题。原因是窗口指定浅色外观，而图层背景在转换为 CGColor 时按系统深色外观取值；现统一使用固定浅色面板背景。
- 新增浅色、深色环境回归检查：修复前深色环境文字对比度约 1.26:1，修复后两种环境均约 19.9:1。内部编辑和页面操作测试继续通过，结果见 `THEME-VALIDATION.txt`。
- 已通过实际桌面窗口确认按钮响应和修复后的面板颜色，并完成工程保存、重启新版、重新打开工程。当前用户工程保存在应用源码目录外。

## 已实现

- A4 固定尺寸、多页、页间距、独立四边页边距、25%～300% 缩放、适合宽度、参考线与水平/垂直平移。
- 新增、复制、删除页面；自由文字框，字体、字号、加粗、斜体、下划线、文字颜色、对齐、行距。
- 图片导入和拖入、跨页移动、等比或自由缩放、位置尺寸输入、图层顺序、多选、网格拼图、可恢复矩形裁剪。
- PNG/JPEG/HEIC 系统编码接口，单图与整页另存、实际字节上限压缩；编码失败不会写入假文件。
- 多文档标签与独立窗口；`.paperdesk` 完整工程保存、另存为、重开、失败保存保护、撤销重做、每文档定时恢复备份。
- 存储空间列表，图片预览与磁盘缓存清理、可选择删除的恢复备份和使用中保护。
- A4 PDF 导出；溢出文字阻止导出，避免静默截断。
- DOCX 与 Markdown 基础导入导出；真正可编辑的文字和图片资源；PDF 仅导出。
- `build.sh`、`run.sh`、`test.sh`、`test-ui.sh`、`test-storage.sh`、本机临时签名的 `.app`、工程格式声明。

## 实际验证

环境：本机 arm64，macOS 26.6.1，Swift 6.3.3。编译目标为 macOS 13+；未在其他系统版本测试。

1. 0.2.0 的 `PAPERDESK_SKIP_HEIC=1 ./test.sh`：**73 项断言、0 个失败组**。HEIC 两组明确跳过，不计为通过。新增旧工程兼容、非对称边距往返、无效边距失败保护、DOCX 边距与最小内容区域精度检查。当前结果见 `FEATURES-VALIDATION.txt`；首版的 52 项结果保留于 `VALIDATION.txt`。
2. 不跳过 HEIC 的测试：系统 `CGImageDestinationFinalize` 失败，伴随 IOSurface 服务错误；未验证成功。不能把它标成已可用的转存功能。
3. 工程往返、错误尺寸、非法裁剪、损坏输入、重复页面 ID、失败保存保护通过。
4. PNG/JPEG 方向与裁剪通过。PNG 上限 25000 字节，结果 20976；JPEG 上限 8000，结果 7936。
5. 两页 PDF 均为 595.276 × 841.890 pt（A4）；不同缩放、页间距和页边距的渲染像素一致；文本层存在；文字溢出会拒绝导出。首版两页样例已用 Poppler 渲染并逐页检查。
6. DOCX 的中英文、嵌入图片和单节四边页边距往返通过，图片遵循内容区域，系统 DOCX 解析器能读取正文。Markdown 本地图片资源及文字往返通过。
7. `./test-ui.sh`：深浅色环境对比度、内部文字编辑、字号、删除、撤销/重做、页面操作、多文档生命周期、页边距和平移通过。0.2.0 离屏界面已检查，实际桌面验证了标签栏加号、⌘T、Control + Tab、⇧⌘N、关闭未保存标签并取消、四边距输入、新文字框位置、中文粘贴、另存为、200%/50% 平移以及存储面板。选中使用中的备份后删除按钮保持禁用，⌘W 只关闭存储面板。
8. `./test-storage.sh`：**18 项通过**，在临时目录检查缓存清理、旧/新恢复目录枚举、使用中备份保护及路径边界，不操作用户的实际缓存。
9. 0.2.0 `.app` 已构建并进行本机临时签名；应用与源码一同打包。实测作业单独保留在项目外的 `work/功能验证.paperdesk`，不混入交付包。

## 首版边界

- 自建文字框需手动分到下一页；导入长文会自动拆分页。复杂 Word 表格、公式、修订、文字环绕等不在基础范围内。
- DOCX / Markdown 按阅读顺序交换内容；自由摆放位置请保存工程，固定版式请导出 PDF。
- 四边页边距属于纸张内部内容区域，不是可自由排列多张纸的外部画布布局；修改边距不自动重排已有文字框和图片。DOCX 只读取基础单节页面边距，Markdown 不保留这些设置。
- PDF 中部分中文字体的 ToUnicode 映射采用同形兼容部首。显示和打印正常；复制文字不保证逐 Unicode 码位往返。已加入实际文本标签，阅读器支持程度有差异。
- 首版交付时电脑控制权限不可用；0.1.1 已实际检查界面配色、按钮响应及工程保存与重开。拖拽、中文输入法等其他交互尚未完成全面实测，内部测试和离屏渲染不能替代这些检查。

## 下一步验收顺序

1. 在正常桌面环境启动 `.app`，用真实 iPhone HEIC 照片验证导入，以及 PNG/JPEG → HEIC、目标大小压缩。若系统编码仍失败，再决定是否加入独立的软件编码组件。
2. 继续验证原有复杂交互：中文输入法组词、对象拖拽及缩放手柄、跨页移动、裁剪、拼图；补充强制退出后多份备份的完整桌面恢复流程。当前已验证恢复文件彼此独立、保存仅移除本份备份；没有为模拟崩溃强制结束用户应用。
3. 用用户日常 DOCX / Markdown 作业样本做兼容性检查。之后按实际需求扩展自动文字续排、复杂表格和更精确的 PDF 复制文本。

继续开发先读 `PLAN.md`、本文件与 `README.md`。源码已完整保留，无须重新搭建工程。
